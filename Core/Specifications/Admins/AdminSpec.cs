using System;
using Core.Entity;

namespace Core.Specifications.Admins
{
  public class AdminSpec
  {
    public AdminSpec(Guid userId, Guid competitionId)
    //: base(x => x.UserId == userId && x.CompId == competitionId)
    {

    }
  }
}

