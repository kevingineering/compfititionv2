using System;
using Core.Entity;

namespace Core.Specifications.Participants
{
  public class ParticipantSpec
  {
    public ParticipantSpec(Guid userId, Guid competitionId)
    //: base(x => x.UserId == userId && x.CompId == competitionId)
    {

    }
  }
}

